import 'package:flutter/material.dart';

class MobileStockView extends StatelessWidget {
  const MobileStockView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}