import 'package:flutter/material.dart';

class MobileStocks extends StatelessWidget {
  const MobileStocks({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Mobile Stocks"),
      ),
    );
  }
}