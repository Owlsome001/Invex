import 'package:flutter/material.dart';

class MobileReportAnalysis extends StatelessWidget {
  const MobileReportAnalysis({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Mobile Report And Analysis"),
      ),
    );
  }
}