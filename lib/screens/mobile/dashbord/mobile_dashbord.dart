import 'package:flutter/material.dart';

class MobileDashbord extends StatelessWidget {
  const MobileDashbord({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Mobile dashboard"),
      ),
    );
  }
}