import 'package:flutter/material.dart';

class DesktopReportAnalysis extends StatelessWidget {
  const DesktopReportAnalysis({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Desktop Report and Analysis"),
      ),
    );
  }
}