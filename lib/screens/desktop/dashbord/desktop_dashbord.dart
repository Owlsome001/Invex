import 'package:flutter/material.dart';
class DesktopDashbord extends StatelessWidget {
  const DesktopDashbord({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Desktop-Dashboard"),
      ),
    );
  }
}