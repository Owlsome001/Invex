import 'package:flutter/material.dart';

class DesktopStocks extends StatelessWidget {
  const DesktopStocks({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Desktop Stocks"),
      ),
    );
  }
}